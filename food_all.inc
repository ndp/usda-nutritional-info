<?include "food_head.inc";	global $gramwts;	$sql = "SELECT NutrDesc, Nutr_Val,Units FROM NUT_DATA, NUTR_DEF WHERE ndb_no=\"$ndb\" AND NUT_DATA.Nutr_No=NUTR_DEF.Nutr_No ORDER BY NUTR_DEF.NutrDesc;";	$result = mysql_query($sql, $conn);	while ($row = mysql_fetch_row($result)) { 		echo "<tr>";		echo "<td>$row[0] ($row[2])</td>";		reset($gramwts);		while (list($key,$value)=each($gramwts)) {			echo "<td align=\"right\">";			echo $row[1]*$value/100; # Nutr. per (100g of Food) / (100 g) * (100g of Food) * g per server = g/serving 			echo " ";			echo "$row[2]";			echo "</td>";		}		echo "</tr>\n";	}?>